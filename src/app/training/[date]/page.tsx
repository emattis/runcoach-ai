export default function WorkoutDetailPage({
  params,
}: {
  params: Promise<{ date: string }>;
}) {
  return (
    <main>
      <h1>Workout Detail</h1>
      <p>Daily workout detail — coming soon</p>
    </main>
  );
}
